fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
python-multipart==0.0.20
Pillow==11.1.0
python-pptx==1.0.2
reportlab==4.2.5
pypdf==5.1.0
# Team-photo feature (generate_team_image): HEIC decode + offline background removal for cut-out posts.
# Optional — the composer falls back to a no-cutout cover-fit if these aren't installed. rembg pulls
# onnxruntime (heavy + a ~176MB model downloaded on first use).
pillow-heif==1.4.0
rembg==2.0.76
onnxruntime==1.27.0
# Animate a post into a short motion clip (animate_asset): MP4 via imageio-ffmpeg (bundles a static
# ffmpeg, ~30MB). Optional — falls back to an animated GIF if imageio-ffmpeg isn't installed.
imageio==2.37.3
imageio-ffmpeg==0.6.0
numpy==2.4.6
