// Views are rendered by the persistent Shell (so state survives navigation).
// Each route just selects which view is shown, via the URL pathname.
export default function ChatPage() {
  return null;
}
